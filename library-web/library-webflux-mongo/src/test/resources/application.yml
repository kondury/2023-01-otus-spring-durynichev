server:
  port: 0
spring:
  messages:
    encoding: UTF-8
  data:
    mongodb:
      host: localhost
      port: 27018
      database: libraryTest
      uri: mongodb://${spring.data.mongodb.host}:${spring.data.mongodb.port}
mongock:
  enabled: false

de:
  flapdoodle:
    mongodb:
      embedded:
        version: "6.0.5"
